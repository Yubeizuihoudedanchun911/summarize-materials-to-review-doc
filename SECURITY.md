# Security Policy

## 支持范围

安全修复以默认分支 `main` 的最新版本为准。历史提交不单独维护。

## 报告安全问题

如果发现可能导致敏感材料泄露、越权访问、危险文件操作或不安全外部写入的问题，请不要创建公开 Issue，也不要在公开 Pull Request 中附带复现数据。

优先通过 GitHub 的私密漏洞报告功能联系仓库所有者；如果该功能不可用，请通过仓库所有者的 GitHub 主页使用可用的私密联系方式。报告中只提供定位问题所需的最少信息，并先对路径、账号、文档内容和凭证进行脱敏。

## 敏感信息处理

本 Skill 的典型输入可能包含个人绩效、业务指标、内部架构、客户信息和受限文档。使用和贡献时必须遵守以下边界：

- 不将真实原始材料或生成文档提交到本仓库。
- 不在日志、Issue、Pull Request 或测试样例中暴露敏感正文。
- 不提交令牌、Cookie、密钥、内部链接或本地绝对路径。
- 示例仅使用虚构身份、项目和数据。
- 在线上传、权限修改和文档密级调整必须获得明确授权。

如果敏感信息被误提交，应立即停止传播并私下联系仓库所有者处理；不要在公开讨论中重复该内容。
